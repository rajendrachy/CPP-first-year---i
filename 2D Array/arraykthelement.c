#include<stdio.h>
int main () {

    int n;
    printf("Enter the size of the array: ");
    scanf("%d", &n);
    printf("Enter the array:");
    int A[n];
    int B[n];
    for(int i=0; i<n; i++) {
        scanf("%d", &A[i]);
    }
    for(int i=0; i<n; i++) {
        scanf("%d", &B[i]);
    }
    int k;
    printf("Enter the kth element : ");
    for(int i=0; i<n; i++) {


    while(n > 0) {
        
    }


    return 0;
}