#include<stdint.h>
#include<string.h>
int main () {
    char *ptr = "college wallah"; //ptr point the C (first character of the string)
    ptr = "physics wallah";
    printf("%s", ptr);
    return 0;
}